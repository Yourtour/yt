package com.yt.websocket;

/*import javax.websocket.CloseReason;
import javax.websocket.OnClose;
import javax.websocket.OnError;
import javax.websocket.OnMessage;
import javax.websocket.OnOpen;
import javax.websocket.Session;
import javax.websocket.server.ServerEndpoint;

@ServerEndpoint("/ws/chat")*/
public class TestWebSocket {

	/*public TestWebSocket() {
		super();
	}

	@OnOpen
	public void onOpen(Session session) {
		System.out.println(String.format("onOpen(): %s.", session.getId()));
		// TODO onOpen
	}

	@OnClose
	public void onClose(Session session, CloseReason reason) {
		System.out.println(String.format("onClose(): %s, reason: %s[%s].",
				session.getId(), reason.getCloseCode().toString(),
				reason.getReasonPhrase()));
		// TODO onClose
	}

	@OnError
	public void onError(Session session, Throwable throwable) {
		System.out.println(String.format("onError(): %s, throwable: %s.",
				session.getId(), throwable.toString()));
		// TODO onError
	}

	@OnMessage
	public void onMessage(Session session, String text) {
		System.out.println(String.format("onMessage(): %s, message: %s.",
				session.getId(), text));
		// TODO onMessage(text)
	}
*/
}
